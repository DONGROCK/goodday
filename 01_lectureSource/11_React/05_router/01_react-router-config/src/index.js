import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <App />
  /*
    리액트 프로젝트를 생성
    npx-create-react-app 프로젝트폴더명 . (여기서  . 의 의미는 현재 폴더에 프로젝트 폴더명으로 생성하겠다.)

    실행
    npm start

    종료
    컨트롤 c

    라이브러리 수정사항이 발생하여 modules를 새로 내려받기 위한
    npm update

    ---------------------------------------------------------------

  */
);

